<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE beans PUBLIC '-//SPRING//DTD BEAN//EN' 'http://www.springframework.org/dtd/spring-beans.dtd'>

<beans>
<!-- 
	<bean id="cabinet_docLib_drive" class="org.alfresco.repo.module.ImporterModuleComponent" parent="module.baseComponent">
    
     <property name="moduleId" value="${project.artifactId}" />
     <property name="name" value="${project.name}" />
     <property name="description" value="Load top level folders" />
     <property name="sinceVersion" value="${project.version}" />
     <property name="appliesFromVersion" value="${project.version}" />
   
    
     <property name="importer" ref="spacesBootstrap"/>
     <property name="bootstrapViews">
      <list>
       <props>
        <prop key="path">/${spaces.company_home.childname}</prop>
        <prop key="location">alfresco/module/${project.artifactId}/bootstrap/top-level-folders-without-full-2a-evi-lib.acp</prop>
       </props>
      </list>
     </property>
   
    </bean>
 -->	
</beans>
